import { Options } from "./interfaces";

export const DEFAULTS: Options = {
  useDotNotationOnKeys: true,
  skipNodes: false
};

export const LOOP = {
  BREAK_CURRENT: 10,
  BREAK_ALL: 11,
  SKIP_CHILDREN: 20
};
